# Use Nginx to serve the static frontend files
FROM nginx:alpine

# Copy your HTML/JS/CSS files into the Nginx public folder
COPY . /usr/share/nginx/html

# Expose port 80 (the default web port)
EXPOSE 80